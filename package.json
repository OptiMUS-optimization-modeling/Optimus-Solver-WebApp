{
  "cacheDirectories": [
    "interface/node_modules"
  ],
  "scripts": {
    "heroku-postbuild": "echo 'Starting heroku postbuild...' && cd interface && npm install && echo 'NPM install completed.' && npm run build && echo 'Build completed.'"
  }
}
